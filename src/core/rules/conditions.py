
from __future__ import annotations

def equals(value, expected)->bool:
    return value == expected

def greater_than(value, threshold)->bool:
    return value > threshold

def less_than(value, threshold)->bool:
    return value < threshold
